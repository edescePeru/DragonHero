@extends('layouts.game') @section('title',$world->name) @php($breadcrumbs=[['label'=>'Inicio','url'=>route('dashboard')],['label'=>'Mundo','url'=>route('worlds.index')],['label'=>$world->name,'url'=>null]]) @section('content')<h1 class="fs-3">{{ $world->name }}</h1><p>{{ $world->description }}</p><div class="card"><div class="card-header">Regiones activas</div><div class="card-body">@foreach($world->regions as $region)<p><a href="{{ route('regions.show',$region) }}">{{ $region->name }}</a></p>@endforeach</div></div>@endsection
