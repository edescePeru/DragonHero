@extends('layouts.game') @section('title','Admin · '.$monster->name) @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages')<div class="d-flex justify-content-between"><h1>{{ $monster->name }}</h1><a class="btn btn-outline-primary" href="{{ route('admin.content.monsters.edit',$monster) }}">Editar estadísticas</a></div><div class="card mb-3"><div class="card-body"><div class="row">@foreach(['code','level','max_health','attack','defense','accuracy_rate','evasion_rate','critical_chance','experience_reward','gold_min','gold_max','status'] as $field)<div class="col-md-3 mb-2"><small>{{ $field }}</small><strong class="d-block">{{ $monster->{$field} }}</strong></div>@endforeach</div></div></div><h2 class="h4">Zonas</h2><ul>@forelse($monster->zones as $zone)<li>{{ $zone->region->world->name }} › {{ $zone->region->name }} › {{ $zone->name }} (peso {{ $zone->pivot->weight }})</li>@empty<li>Sin zonas</li>@endforelse</ul><h2 class="h4">Loot configurado</h2><table class="table"><thead><tr><th>Item</th><th>Probabilidad</th><th>Min</th><th>Max</th><th>Estado</th><th></th></tr></thead><tbody>@foreach($monster->lootEntries as $entry)<tr><td>{{ $entry->item->name }}</td><td>{{ $entry->drop_chance_basis_points }} = {{ $read->percent($entry->drop_chance_basis_points) }}%</td><td>{{ $entry->minimum_quantity }}</td><td>{{ $entry->maximum_quantity }}</td><td>{{ $entry->status }}</td><td><form method="POST" action="{{ route('admin.content.monsters.loot.destroy',[$monster,$entry]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-warning">Desactivar</button></form></td></tr>@endforeach</tbody></table><h3 class="h5">Agregar Item</h3><form method="POST" action="{{ route('admin.content.monsters.loot.store',$monster) }}" class="row g-2">@csrf<div class="col-md-4"><select class="form-select" name="item_id">@foreach($items as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select></div>@foreach(['drop_chance_basis_points'=>10000,'minimum_quantity'=>1,'maximum_quantity'=>1,'sort_order'=>0] as $field=>$default)<div class="col-md-2"><input class="form-control" type="number" name="{{ $field }}" value="{{ old($field,$default) }}" placeholder="{{ $field }}"></div>@endforeach<div class="col-md-2"><select class="form-select" name="status">@foreach($statuses as $status)<option>{{ $status }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary">Agregar</button></div></form>@endsection
