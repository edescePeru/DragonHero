<form method="POST" action="{{ $action }}" class="mt-3 js-loot-form">@csrf @if($method!=='POST') @method($method) @endif
<div class="mb-3"><label class="form-label">Item</label><select class="form-select @error('item_id') is-invalid @enderror" name="item_id">@foreach($items as $item)<option value="{{ $item->id }}" @if((int)old('item_id',$entry ? $entry->item_id : 0)===(int)$item->id) selected @endif>{{ $item->name }} · {{ $item->code }} · {{ $item->item_type }} · {{ $item->is_stackable?'apilable':'no apilable' }}</option>@endforeach</select><div class="form-text">Objeto que el monstruo puede entregar al ser derrotado.</div>@error('item_id')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
<div class="mb-3"><label class="form-label">Probabilidad (%)</label><input class="form-control @error('drop_probability_percent') is-invalid @enderror" name="drop_probability_percent" type="text" inputmode="decimal" value="{{ old('drop_probability_percent',$probabilityPercent) }}" placeholder="Ej. 0.0001"><div class="form-text">Admite hasta 4 decimales. 100 = 100 %, 0.0001 = 1 PPM. Valor interno actual: {{ $entry ? $entry->drop_probability_ppm : 1000000 }} PPM.</div>@error('drop_probability_percent')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
<div class="row g-3"><div class="col-md-6"><label class="form-label">Cantidad mínima</label><input class="form-control @error('minimum_quantity') is-invalid @enderror" name="minimum_quantity" type="number" min="1" value="{{ old('minimum_quantity',$entry ? $entry->minimum_quantity : 1) }}" placeholder="Ej. 1"><div class="form-text">Cantidad mínima entregada cuando el drop resulta exitoso.</div>@error('minimum_quantity')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label">Cantidad máxima</label><input class="form-control @error('maximum_quantity') is-invalid @enderror" name="maximum_quantity" type="number" min="1" value="{{ old('maximum_quantity',$entry ? $entry->maximum_quantity : 1) }}" placeholder="Ej. 3"><div class="form-text">Cantidad máxima entregada cuando el drop resulta exitoso. Debe ser igual o mayor que la mínima.</div>@error('maximum_quantity')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label">Estado</label><select class="form-select @error('status') is-invalid @enderror" name="status">@foreach($statuses as $status)<option @if(old('status',$entry ? $entry->status : 'active')===$status) selected @endif>{{ $status }}</option>@endforeach</select><div class="form-text">Active permite que el Item pueda caer. Inactive conserva la configuración, pero evita que el monstruo lo entregue.</div>@error('status')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label">Orden</label><input class="form-control @error('sort_order') is-invalid @enderror" name="sort_order" type="number" min="0" value="{{ old('sort_order',$entry ? $entry->sort_order : 0) }}" placeholder="Ej. 10"><div class="form-text">Controla el orden en que aparece esta entrada. No modifica su probabilidad de caída.</div>@error('sort_order')<div class="invalid-feedback">{{ $message }}</div>@enderror</div></div><button class="btn btn-primary mt-3" type="submit">{{ $buttonLabel }}</button></form>
