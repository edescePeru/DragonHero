@extends('layouts.game') @section('title','Admin · '.$row['model']->name) @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages') @php($template=$row['model'])<div class="d-flex justify-content-between"><div><h1>{{ $template->name }}</h1><p>{{ $template->code }} · {{ $template->characterClass->name }} · {{ $template->body_type }}</p></div><a class="btn btn-outline-primary" href="{{ route('admin.content.character-templates.edit',$template) }}">Editar</a></div>@if(!$row['class_available'])<div class="alert alert-warning">Esta plantilla no está disponible porque su clase está inactiva.</div>@endif<div class="row"><div class="col-md-5"><img src="{{ $row['visual']->url512() }}" width="256" height="384" class="img-fluid" style="object-fit:contain" alt="Cuerpo completo de {{ $template->name }}"></div><div class="col-md-7"><h2 class="h5">Estadísticas iniciales</h2><dl><dt>Vida</dt><dd>{{ $template->base_max_health }}</dd><dt>Ataque / Defensa</dt><dd>{{ $template->base_attack }} / {{ $template->base_defense }}</dd><dt>Precisión / Evasión / Crítico</dt><dd>{{ $template->base_accuracy }} / {{ $template->base_evasion }} / {{ $template->base_critical_rate }}%</dd><dt>Estado</dt><dd>{{ $template->status }}</dd></dl><div class="d-flex gap-2">@if($template->status!=='active')<form method="POST" action="{{ route('admin.content.character-templates.activate',$template) }}">@csrf @method('PATCH')<button class="btn btn-success">Activar</button></form>@else<form method="POST" action="{{ route('admin.content.character-templates.deactivate',$template) }}">@csrf @method('PATCH')<button class="btn btn-warning">Inactivar</button></form>@endif<form method="POST" action="{{ route('admin.content.character-templates.hide',$template) }}">@csrf @method('PATCH')<button class="btn btn-outline-secondary">Ocultar</button></form></div></div></div>@endsection
