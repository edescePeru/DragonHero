@extends('layouts.game') @section('title','Admin · Items') @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages')<div class="d-flex justify-content-between"><h1>Items</h1><a class="btn btn-primary" href="{{ route('admin.content.items.create') }}">Crear Item</a></div><div class="table-responsive"><table class="table"><thead><tr><th>Código</th><th>Nombre</th><th>Tipo</th><th>Rareza</th><th>Stack</th><th>Clasificación</th><th>Equipment</th><th>Stats</th><th>Estado</th><th></th></tr></thead><tbody>@foreach($items as $row)@php($item=$row['model'])<tr><td>{{ $item->code }}</td><td>{{ $item->name }}</td><td>{{ $item->item_type }}</td><td>{{ $item->rarity }}</td><td>{{ $item->is_stackable?'sí':'no' }} / {{ $item->max_stack }}</td><td>{{ $row['classification'] }}</td><td>{{ $item->equipment_type ?: '—' }} · {{ $row['coherent']?'coherente':'incoherente' }}</td><td>@foreach($row['bonuses'] as $name=>$value)<span class="d-block">{{ $name }}: {{ $value }}</span>@endforeach</td><td>{{ $item->status }}</td><td><a href="{{ route('admin.content.items.edit',$item) }}">Editar</a><form method="POST" action="{{ route('admin.content.items.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-warning">Desactivar</button></form></td></tr>@endforeach</tbody></table></div>{{ $items->links() }}@endsection
