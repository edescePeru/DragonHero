@extends('layouts.game')
@section('title','Admin · Refinamiento')
@section('content')
@include('admin.content.partials.navigation') @include('admin.content.partials.messages')
<h1>Reglas de refinamiento</h1><p class="text-secondary">Refinamiento v1 permite únicamente 10000 puntos base (100 %) y conserva el nivel ante fallos futuros.</p>
<div class="card mb-4"><div class="card-body"><form method="POST" action="{{ route('admin.content.refinement.store') }}" class="row g-2">@csrf<div class="col"><input class="form-control" name="from_level" type="number" min="0" max="14" placeholder="Desde"></div><div class="col"><input class="form-control" name="to_level" type="number" min="1" max="15" placeholder="Hasta"></div><div class="col"><input class="form-control" name="gold_cost" type="number" min="0" value="0"></div><input type="hidden" name="success_chance_basis_points" value="10000"><input type="hidden" name="failure_behavior" value="keep_level"><div class="col"><select class="form-select" name="status"><option>active</option><option>inactive</option><option>hidden</option></select></div><div class="col"><button class="btn btn-primary">Crear regla</button></div></form></div></div>
@foreach($rules as $rule)<div class="card mb-3"><div class="card-header">+{{ $rule->from_level }} → +{{ $rule->to_level }} · {{ $rule->gold_cost }} oro · {{ $rule->success_chance_basis_points }} BP · {{ $rule->status }}</div><div class="card-body"><form method="POST" action="{{ route('admin.content.refinement.update',$rule) }}" class="row g-2 mb-3">@csrf @method('PUT')<input type="hidden" name="from_level" value="{{ $rule->from_level }}"><div class="col"><input class="form-control" name="to_level" value="{{ $rule->to_level }}" type="number"></div><div class="col"><input class="form-control" name="gold_cost" value="{{ $rule->gold_cost }}" type="number"></div><div class="col"><input class="form-control" name="success_chance_basis_points" value="10000" readonly></div><input type="hidden" name="failure_behavior" value="keep_level"><div class="col"><select class="form-select" name="status">@foreach(['active','inactive','hidden'] as $status)<option @if($rule->status===$status) selected @endif>{{ $status }}</option>@endforeach</select></div><div class="col"><button class="btn btn-outline-primary">Guardar</button></div></form><ul>@foreach($rule->materials as $line)<li>{{ $line->item->name }} × {{ $line->quantity }} <form class="d-inline" method="POST" action="{{ route('admin.content.refinement.materials.destroy',[$rule,$line]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-link">Quitar</button></form></li>@endforeach</ul><form method="POST" action="{{ route('admin.content.refinement.materials.store',$rule) }}" class="row g-2">@csrf<div class="col"><select class="form-select" name="item_id">@foreach($materials as $item)<option value="{{ $item->id }}">{{ $item->name }}</option>@endforeach</select></div><div class="col"><input class="form-control" name="quantity" type="number" min="1" value="1"></div><div class="col"><button class="btn btn-outline-secondary">Agregar material</button></div></form></div></div>@endforeach
<hr class="my-5"><h2>Bonus estadístico por nivel</h2><p>Esta configuración es independiente de costes y materiales.</p><div class="alert alert-info">Este porcentaje es el incremento total aplicado sobre los bonus originales del Item para este nivel. No se suma con los porcentajes de niveles anteriores y no agrega estadísticas que el Item no posea.</div><div class="card mb-3"><div class="card-body"><strong>Ejemplo:</strong> Item con Ataque +10 · +3 con 3000 BP · Bonus de refinamiento +3 · Aporte total +13.</div></div>
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Nivel</th><th>BP</th><th>Porcentaje</th><th>Estado</th><th>Configuración</th><th>Acciones</th></tr></thead><tbody>@foreach($statLevels as $row)<tr class="{{ !$row['active']?'table-warning':'' }}"><td>+{{ $row['level'] }}</td>@if($row['modifier'])<td>{{ $row['modifier']->stat_increase_basis_points }}</td><td>{{ $row['percent'] }}%</td><td>{{ $row['modifier']->status }}</td><td>@if($row['active'])<span class="badge bg-success">Activa</span>@else<span class="badge bg-warning text-dark">Sin configuración estadística activa</span>@endif</td><td><form class="row g-1 mb-1" method="POST" action="{{ route('admin.content.refinement.stats.update',$row['modifier']) }}">@csrf @method('PUT')<input type="hidden" name="refinement_level" value="{{ $row['level'] }}"><div class="col"><input class="form-control form-control-sm" name="stat_increase_basis_points" type="number" min="0" max="100000" value="{{ $row['modifier']->stat_increase_basis_points }}"></div><div class="col"><select class="form-select form-select-sm" name="status">@foreach(['active','inactive','hidden'] as $status)<option @if($row['modifier']->status===$status) selected @endif>{{ $status }}</option>@endforeach</select></div><div class="col"><button class="btn btn-sm btn-outline-primary">Guardar</button></div></form>@if($row['active'])<form method="POST" action="{{ route('admin.content.refinement.stats.deactivate',$row['modifier']) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-warning">Desactivar</button></form>@else<form method="POST" action="{{ route('admin.content.refinement.stats.activate',$row['modifier']) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success">Activar</button></form>@endif</td>@else<td colspan="3"><span class="badge bg-warning text-dark">Faltante · Sin configuración estadística activa</span></td><td><form class="row g-1" method="POST" action="{{ route('admin.content.refinement.stats.store') }}">@csrf<input type="hidden" name="refinement_level" value="{{ $row['level'] }}"><input type="hidden" name="status" value="active"><div class="col"><input class="form-control form-control-sm" name="stat_increase_basis_points" type="number" min="0" max="100000" placeholder="Ej. {{ $row['level']*1000 }}"></div><div class="col"><button class="btn btn-sm btn-primary">Crear</button></div></form></td>@endif</tr>@endforeach</tbody></table></div>
<p class="small text-secondary">1000 BP = 10% · 5000 BP = 50% · 10000 BP = 100% · 15000 BP = 150% · máximo 100000 BP = 1000% adicional.</p>
@endsection
