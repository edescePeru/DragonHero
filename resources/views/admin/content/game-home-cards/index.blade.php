@extends('layouts.game') @section('title','Admin · Menú de inicio') @section('content')
@include('admin.content.partials.navigation') @include('admin.content.partials.messages')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h3 mb-1">Menú de inicio</h1><p class="text-secondary mb-0">Accesos visuales administrables.</p></div><a class="btn btn-primary" href="{{ route('admin.content.game-home-cards.create') }}">Nueva card</a></div>
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>Orden</th><th>Card</th><th>Destino</th><th>Banner</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($cards as $card)<tr><td>{{ $card->sort_order }}</td><td><strong>{{ $card->name }}</strong><br><code>{{ $card->code }}</code></td><td>{{ $card->destination_type }}<br><small>{{ $card->destination_value }}</small></td><td>@if($card->mediaAssets->first())<img src="{{ $card->mediaAssets->first()->url() }}" width="180" height="60" style="object-fit:cover" alt="Banner de {{ $card->name }}">@else<span class="text-secondary">Fallback</span>@endif</td><td>{{ $card->is_active?'Activa':'Inactiva' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.content.game-home-cards.edit',$card) }}">Editar</a><form class="d-inline" method="POST" action="{{ route($card->is_active?'admin.content.game-home-cards.deactivate':'admin.content.game-home-cards.activate',$card) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">{{ $card->is_active?'Desactivar':'Activar' }}</button></form></td></tr>@empty<tr><td colspan="6">No hay cards.</td></tr>@endforelse</tbody></table></div>
@endsection
