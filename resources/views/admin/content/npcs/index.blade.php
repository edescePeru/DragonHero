@extends('layouts.game') @section('title','Admin · NPCs') @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages')<div class="d-flex justify-content-between align-items-center"><h1>NPCs</h1><a class="btn btn-primary" href="{{ route('admin.content.npcs.create') }}">Crear NPC</a></div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Retrato</th><th>Código</th><th>Nombre</th><th>Estado</th><th>Shops</th><th>Actualizado</th><th></th></tr></thead><tbody>@foreach($npcs as $row)<tr><td>@if($row['portrait']['exists'])<img src="{{ $row['portrait']['url'] }}" alt="" width="56" height="56" class="rounded object-fit-cover">@else—@endif</td><td>{{ $row['model']->code }}</td><td>{{ $row['model']->name }}</td><td>{{ $row['model']->status }}</td><td>{{ $row['model']->shops_count }}</td><td>{{ $row['model']->updated_at }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.content.npcs.edit',$row['model']) }}">Editar</a></td></tr>@endforeach</tbody></table></div>{{ $npcs->links() }}@endsection
