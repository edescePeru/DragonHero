@extends('layouts.game') @section('title','Admin · Zones') @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages')<div class="d-flex justify-content-between"><h1>Zones</h1><a class="btn btn-primary" href="{{ route('admin.content.zones.create') }}">Crear Zone</a></div><table class="table"><thead><tr><th>Jerarquía</th><th>Niveles</th><th>Tipo</th><th>Estado</th><th></th></tr></thead><tbody>@foreach($zones as $zone)<tr><td>{{ $zone->region->world->name }} › {{ $zone->region->name }} › <a href="{{ route('admin.content.zones.show',$zone) }}">{{ $zone->name }}</a></td><td>{{ $zone->recommended_level_min }}–{{ $zone->recommended_level_max ?: '∞' }}</td><td>{{ $zone->zone_type }}</td><td>{{ $zone->status }}</td><td><a href="{{ route('admin.content.zones.edit',$zone) }}">Editar</a></td></tr>@endforeach</tbody></table>{{ $zones->links() }}@endsection
