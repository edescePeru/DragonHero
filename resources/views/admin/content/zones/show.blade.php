@extends('layouts.game') @section('title','Admin · '.$zone->name) @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages')<h1>{{ $zone->region->world->name }} › {{ $zone->region->name }} › {{ $zone->name }}</h1><p>Niveles {{ $zone->recommended_level_min }}–{{ $zone->recommended_level_max ?: '∞' }} · {{ $zone->status }}</p><h2 class="h4">Monsters asignados</h2><table class="table"><thead><tr><th>Monster</th><th>Peso</th><th>Min</th><th>Max</th><th>Estado</th><th></th></tr></thead><tbody>@foreach($zone->monsters as $monster)<tr><form method="POST" action="{{ route('admin.content.zones.monsters.update',[$zone,$monster]) }}">@csrf @method('PUT')<input type="hidden" name="monster_id" value="{{ $monster->id }}"><td>{{ $monster->name }}</td><td><input class="form-control" name="weight" value="{{ $monster->pivot->weight }}"></td><td><input class="form-control" name="minimum_character_level" value="{{ $monster->pivot->minimum_character_level }}"></td><td><input class="form-control" name="maximum_character_level" value="{{ $monster->pivot->maximum_character_level }}"></td><td><select class="form-select" name="status">@foreach($statuses as $status)<option @if($monster->pivot->status===$status) selected @endif>{{ $status }}</option>@endforeach</select></td><td><button class="btn btn-sm btn-primary">Actualizar</button></form><form method="POST" action="{{ route('admin.content.zones.monsters.destroy',[$zone,$monster]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Quitar</button></form></td></tr>@endforeach</tbody></table><h3 class="h5">Agregar Monster</h3><form method="POST" action="{{ route('admin.content.zones.monsters.store',$zone) }}" class="row g-2">@csrf<div class="col-md-4"><select class="form-select" name="monster_id">@foreach($monsters as $monster)<option value="{{ $monster->id }}">{{ $monster->name }}</option>@endforeach</select></div><div class="col-md-2"><input class="form-control" name="weight" value="100"></div><div class="col-md-2"><input class="form-control" name="minimum_character_level" value="1"></div><div class="col-md-2"><input class="form-control" name="maximum_character_level"></div><div class="col-md-2"><select class="form-select" name="status">@foreach($statuses as $status)<option>{{ $status }}</option>@endforeach</select></div><button class="btn btn-primary">Agregar</button></form>@endsection
