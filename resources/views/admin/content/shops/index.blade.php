@extends('layouts.game') @section('title','Admin · Tiendas') @section('content') @include('admin.content.partials.navigation') @include('admin.content.partials.messages')<div class="d-flex justify-content-between"><h1>Tiendas</h1><a class="btn btn-primary" href="{{ route('admin.content.shops.create') }}">Crear Shop</a></div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Banner</th><th>Código</th><th>Nombre</th><th>NPC</th><th>Localizaciones</th><th>Estado</th><th>Ofertas</th><th></th></tr></thead><tbody>@foreach($shops as $row)<tr><td>@if($row['banner']['exists'])<img src="{{ $row['banner']['url'] }}" alt="" width="96" height="48" class="rounded object-fit-cover">@else—@endif</td><td>{{ $row['model']->code }}</td><td>{{ $row['model']->name }}</td><td>{{ $row['model']->npc->name }}</td><td>{{ count($row['locations'])?implode(', ',$row['locations']):'Global' }}</td><td>{{ $row['model']->status }} · {{ $row['available']?'disponible':'no disponible' }}</td><td>{{ $row['model']->offers_count }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.content.shops.edit',$row['model']) }}">Editar</a></td></tr>@endforeach</tbody></table></div>{{ $shops->links() }}@endsection
