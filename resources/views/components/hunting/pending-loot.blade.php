<section class="card hunting-loot" aria-labelledby="pending-loot-title"><div class="card-body"><div class="d-flex flex-wrap justify-content-between gap-2"><h2 class="h4 mb-0" id="pending-loot-title">Loot encontrado</h2><form method="POST" action="{{ route('characters.hunt-rewards.claim',$character) }}" id="claim-rewards-form">@csrf<button class="btn btn-primary" type="submit">Reclamar todas</button></form></div><div class="hunting-item-grid mt-3" id="pending-rewards-items">@foreach($summary['items'] as $item)<div class="hunting-item-card"><div class="hunting-item-image">@if($item['image_url'])<img src="{{ $item['image_url'] }}" alt="" loading="lazy">@else<span aria-hidden="true">◇</span>@endif</div><span>{{ $item['item_name'] }}</span><strong>×{{ $item['quantity'] }}</strong></div>@endforeach</div><p class="mb-0 mt-3"><strong>Oro pendiente:</strong> <span id="pending-gold">{{ $summary['gold_amount'] }}</span> · <strong>Experiencia pendiente:</strong> <span id="pending-experience">{{ $summary['experience_amount'] }}</span></p><p class="text-secondary mb-0" id="claim-empty-message"></p></div></section>
