<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><title>Seleccionar personaje - DragonHero</title><meta name="viewport" content="width=device-width, initial-scale=1"><script type="module" src="{{ asset('assets/js/main.js') }}"></script><link rel="stylesheet" href="{{ asset('assets/css/main.css') }}"><style>.hero-visual{width:256px;height:384px;max-width:100%;object-fit:contain}.hero-slot{min-height:620px}.hero-selected{outline:3px solid var(--bs-success)}</style></head><body><main class="container py-5"><div class="text-center mb-4"><h1>Selecciona tu personaje</h1><p class="text-secondary">Tu cuenta dispone de {{ $limit }} espacios independientes.</p></div>@if($errors->any())<div class="alert alert-warning">{{ $errors->first() }}</div>@endif<div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">@foreach($slots as $slot)<div class="col">@if($slot)<article class="card hero-slot h-100 text-center p-3 {{ $slot['selected']?'hero-selected':'' }}"><img class="hero-visual mx-auto" width="256" height="384" src="{{ $slot['visual']->url256() }}" alt="Cuerpo completo de {{ $slot['character']->name }}"><h2 class="h4 mt-3">{{ $slot['character']->name }}</h2><p>{{ $slot['character']->characterClass->name }} · Nivel {{ $slot['character']->level }}</p><div class="progress" role="progressbar" aria-label="Progreso de experiencia" aria-valuenow="{{ $slot['progress']->percentage() }}" aria-valuemin="0" aria-valuemax="100"><div class="progress-bar" style="width:{{ $slot['progress']->percentage() }}%">{{ $slot['progress']->percentage() }}%</div></div>@if($slot['selected'])<span class="badge bg-success mt-3">Seleccionado actualmente</span>@endif<form method="POST" action="{{ route('characters.select.store',$slot['character']) }}" class="mt-auto pt-3">@csrf<button class="btn btn-primary w-100">Entrar</button></form></article>@else<article class="card hero-slot h-100 text-center p-4 d-flex justify-content-center"><div aria-hidden="true" style="font-size:8rem;opacity:.25">♙</div><h2 class="h4">Crear nuevo personaje</h2><p class="text-secondary">Este espacio está disponible.</p><a class="btn btn-outline-primary" href="{{ route('characters.create') }}">Crear personaje</a></article>@endif</div>@endforeach</div>@if($atLimit)<div class="alert alert-info mt-4 text-center">Has alcanzado el máximo de {{ $limit }} personajes.</div>@endif<form method="POST" action="{{ route('logout') }}" class="text-center mt-4">@csrf<button class="btn btn-link text-secondary">Cerrar sesión</button></form></main></body></html>
