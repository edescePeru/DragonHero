@extends('layouts.game') @section('title',$region->name) @section('content')<div class="mb-4"><a href="{{ route('worlds.show',$region->world) }}">{{ $region->world->name }}</a><h1 class="fs-3 mt-2">{{ $region->name }}</h1><p>{{ $region->description }}</p></div><div class="row g-3">@foreach($region->zones as $zone)<div class="col-lg-4"><div class="card h-100"><div class="card-body p-4"><span class="badge bg-primary-subtle text-primary">{{ $zone->zone_type }}</span><h2 class="h5 mt-3">{{ $zone->name }}</h2><p>Nivel {{ $zone->recommended_level_min }} - {{ $zone->recommended_level_max ?: 'Sin límite' }}</p><a href="{{ route('zones.show',$zone) }}">Ver zona</a></div></div></div>@endforeach</div>@endsection
