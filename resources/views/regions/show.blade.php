@extends('layouts.game') @section('title',$region->name) @php($breadcrumbs=[['label'=>'Inicio','url'=>route('dashboard')],['label'=>'Mundo','url'=>route('worlds.index')],['label'=>$region->world->name,'url'=>route('worlds.show',$region->world)],['label'=>$region->name,'url'=>null]]) @section('content')<h1 class="fs-3">{{ $region->name }}</h1><p>{{ $region->description }}</p><div class="row g-3">@foreach($region->zones as $zone)<div class="col-lg-4"><div class="card"><div class="card-body"><h2 class="h5">{{ $zone->name }}</h2><a href="{{ route('zones.show',$zone) }}">Ver zona</a></div></div></div>@endforeach</div>@endsection
